extends ../layout

block content
  h1= title
  ul
  //- each-in-else语法
  each book in book_list
    li 
      a(href=book.url) #{book.title} 
      |  (#{book.author.name})
  else
    li There are no books.
